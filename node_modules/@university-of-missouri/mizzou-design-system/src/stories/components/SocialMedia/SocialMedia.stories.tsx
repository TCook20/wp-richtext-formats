import React from 'react'
import { Story } from '@storybook/react'

import SocialMedia, { SocialMediaProps } from 'Components/SocialMedia/SocialMedia'
import { description, status } from 'Components/SocialMedia/config.json'
import mdx from './SocialMedia.mdx'

const Template: Story<SocialMediaProps> = (args) => {
  return <SocialMedia {...args} />
}

export default {
  component: SocialMedia,
  title: 'Design System/Components/Social Media',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    attributes: {
      table: {
        disable: true,
      },
    },
  },
}

export const socialMedia = Template.bind({})
socialMedia.args = {
  size: 'md',
  color: 'default',
  name: 'twitter',
}
