import React from 'react'
import { Story } from '@storybook/react'

import Nav, { NavProps } from 'Components/Nav/Nav'
import { description, status } from 'Components/Nav/config.json'
import NavContext from './Nav.context'
import mdx from './Nav.mdx'

const Template: Story<NavProps> = (args) => {
  return <Nav {...args} />
}

export default {
  component: Nav,
  title: 'Design System/Components/Navs',
  argTypes: {},
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
}
export const Navs = Template.bind({})
Navs.args = {
  ...NavContext.default,
}
