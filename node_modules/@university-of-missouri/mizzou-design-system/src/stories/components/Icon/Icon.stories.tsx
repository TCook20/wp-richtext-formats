import React from 'react'
import { Story } from '@storybook/react'

import Icon, { IconProps, IconSvg, IconSvgProps } from 'Components/Icon/Icon'
import { description, status } from 'Components/Icon/config.json'
import mdx from './Icon.mdx'

const Template: Story<IconProps> = (args) => {
  return <Icon {...args} />
}

const SVGTemplate: Story<IconSvgProps> = (args) => {
  return <IconSvg {...args} />
}

export default {
  component: Icon,
  title: 'Design System/Components/Icons',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    children: {
      table: {
        category: 'Props',
        disable: true,
      },
    },
    attributes: {
      table: {
        disable: true,
      },
    },
    name: {
      table: {
        category: 'Options',
      },
    },
    size: {
      table: {
        category: 'Options',
      },
    },
    color: {
      table: {
        category: 'Options',
      },
    },
  },
}
export const Icons = Template.bind({})
Icons.args = {
  name: 'search',
  size: 'md',
  color: 'black',
}

export const SVG = SVGTemplate.bind({})
SVG.args = {
  name: 'search',
  size: 'md',
  color: 'black',
}
