import {
  Title,
  Description,
  Primary,
  ArgsTable,
  Stories,
  PRIMARY_STORY,
} from '@storybook/addon-docs'
import { status } from 'Components/Navigation/Primary/config.json'
import { StatusGroup } from 'SB/blocks/ComponentStatus/ComponentStatus.tsx'

<StatusGroup statuses={status} />

<Title />

<Description />

<div className="miz-fill--gold border miz-space--inset-sm">

**Note:** This component is still in a &ldquo;prototype&rdquo; state until it can be tested further. Wherever possible, we will try to keep the core functionality the same going forward and will communicate any potential breaking changes before updating the System.

</div>

<Primary />
<ArgsTable story={PRIMARY_STORY} />
<Stories />

- [Usage](#usage)
- **Options reference**
  - [Options](#options)

## Usage

```twig
{% set navigationOptions = {
  list: {
    items: [
      {
        link: {
          text: "Home",
          href: "#"
        }
      },
      {...},
      {...}
    ]
  }
} %}


{% include "@miz/components/navigation/primary/primary.twig" with navigationOptions only %}
```

## Options reference

### Options

| Param        | Default        | Values | Description                                    |
| ------------ | -------------- | ------ | ---------------------------------------------- |
| `class`      | `miz-masthead` | String | Additional classes appended to the masthead    |
| `attributes` | `null`         | Object | Attributes to be applied to the masthead       |
| `list`       | `{}`           | Object | Contains an array of items (See [List](#list)) |

### `List`

Structure for the `list` object. This structure matches the same [list object found in the **Navs**](/docs/design-system-components-navs--navs) component

To produce **one** navigation item, you would structure your `list` object like this:

```js
{
  "list": {
    "items": [
      {
        "link": {
          "text": `"One nav item"`,
          "href": `"#"`
        }
      }
    ]
  }
}
```
