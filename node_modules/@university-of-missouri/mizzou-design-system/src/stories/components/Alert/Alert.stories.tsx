import React from 'react'
import { Story } from '@storybook/react'

import Alert, { AlertProps } from 'Components/Alert/Alert'
import { description, status } from 'Components/Alert/config.json'
import mdx from './Alert.mdx'

const Template: Story<AlertProps> = (args) => {
  return <Alert {...args} />
}

export default {
  component: Alert,
  title: 'Design System/Components/Alert',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    children: {
      table: {
        category: 'Props',
      },
    },
    attributes: {
      table: {
        disable: true,
      },
    },
    link: {
      table: {
        category: 'Options',
      },
    },
    variant: {
      table: {
        category: 'Options',
      },
    },
  },
}
export const alert = Template.bind({})
alert.args = {
  children: <p>Alert Content</p>,
  variant: 'info',
  link: '',
}
