import React from 'react'
import { Story } from '@storybook/react'

import Masthead, { MastheadProps } from 'Components/Masthead/Masthead'
import { description, status } from 'Components/Masthead/config.json'
import mdx from './Masthead.mdx'

const Template: Story<MastheadProps> = (args) => {
  return <Masthead {...args} />
}

export default {
  component: Masthead,
  title: 'Design System/Components/Masthead',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    title: {
      table: {
        category: 'Props',
      },
    },
    subtitle: {
      table: {
        category: 'Props',
      },
    },
    attributes: {
      table: {
        disable: true,
      },
    },
  },
}

export const masthead = Template.bind({})
masthead.args = {
  title: 'Default Site Name',
  subtitle: '',
  branded: false,
}
