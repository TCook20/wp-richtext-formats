import React from 'react'
import { Story } from '@storybook/react'

import Header, { HeaderProps } from 'Components/Header/Header'
import { description, status } from 'Components/Header/config.json'
import mdx from './Header.mdx'

const Template: Story<HeaderProps> = (args) => {
  return <Header {...args} />
}

export default {
  component: Header,
  title: 'Design System/Components/Header',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    attributes: {
      table: {
        disable: true,
      },
    },
    theme: {
      options: ['light', 'dark'],
      table: {
        category: 'Options',
      },
    },
  },
}

export const header = Template.bind({})
header.args = {
  theme: 'light',
}
