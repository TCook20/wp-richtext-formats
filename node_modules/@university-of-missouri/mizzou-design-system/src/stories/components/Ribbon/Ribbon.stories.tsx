import React from 'react'
import { Story } from '@storybook/react'

import Ribbon, { RibbonProps } from 'Components/Ribbon/Ribbon'
import { description, status } from 'Components/Ribbon/config.json'
import mdx from './Ribbon.mdx'

const Template: Story<RibbonProps> = (args) => {
  return <Ribbon {...args} />
}

export default {
  component: Ribbon,
  title: 'Design System/Components/Ribbon',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    attributes: {
      table: {
        disable: true,
      },
    },
    linkList: {
      table: {
        category: 'Options',
      },
    },
  },
}

export const Default = Template.bind({})
Default.args = {}

export const WithLinks = Template.bind({})
WithLinks.args = {
  linkList: [
    { text: 'List Item 1', url: '#' },
    { text: 'List Item 1', url: '#' },
    { text: 'List Item 1', url: '#' },
  ],
}
