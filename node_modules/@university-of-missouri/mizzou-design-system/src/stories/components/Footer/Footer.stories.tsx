import React from 'react'
import { Story } from '@storybook/react'

import Footer, { FooterProps } from 'Components/Footer/Footer'
import { description, status } from 'Components/Footer/config.json'
import mdx from './Footer.mdx'

const Template: Story<FooterProps> = (args) => {
  return <Footer {...args} />
}

export default {
  component: Footer,
  title: 'Design System/Components/Footer',
  parameters: {
    docs: {
      description: {
        component: description,
      },
      page: mdx,
    },
    status,
  },
  argTypes: {
    attributes: {
      table: {
        disable: true,
      },
    },
    theme: {
      options: ['light', 'dark'],
      table: {
        category: 'Options',
      },
    },
  },
}

export const footer = Template.bind({})
footer.args = {
  theme: 'light',
}
