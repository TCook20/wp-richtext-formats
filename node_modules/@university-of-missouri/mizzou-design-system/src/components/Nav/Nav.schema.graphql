type Attributes {
    id: String
}

interface Element {
    attributes: Attributes
    className: String
}

type Nav implements Element {
    name: String!
    list: NavList!
}

type NavList implements Element {
    items: [NavItem]!
}

type NavItem implements Element {
    content: Content!
}

scalar Content
