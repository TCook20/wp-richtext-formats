/**
 * @format
 */
const stylelintRules = require('./rules')
const stylelintScssRules = require('./rules/scss')
const mizzouBrowserslistConfig = require('@university-of-missouri/browserslist-config')

module.exports = {
  extends: ['stylelint-config-standard'],
  plugins: ['stylelint-no-unsupported-browser-features'],
  defaultSeverity: 'warning',
  rules: {
    // Default rules
    ...stylelintRules,

    // No unsupported browser features
    'plugin/no-unsupported-browser-features': [
      true,
      {
        severity: 'warning',
        browsers: mizzouBrowserslistConfig,
        ignore: ['css-gradients'],
      },
    ],
  },
  overrides: [
    {
      extends: ['stylelint-config-standard-scss'],
      files: ['**/*.scss'],
      plugins: ['stylelint-scss'],
      // SCSS specific rules
      rules: stylelintScssRules,
    },
  ],
}
